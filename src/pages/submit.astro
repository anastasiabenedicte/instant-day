---
import MainLayout from "../layouts/MainLayout.astro";
---

<MainLayout>
  <section class="bg-accent min-h-screen flex items-center justify-center px-4 py-10 md:py-0">
    <!-- Centered white box -->
    <div class="w-full max-w-6xl bg-secondary p-6 md:p-12 grid grid-cols-1 md:grid-cols-2 gap-12">
      
      <!-- Instructions -->
      <div class="text-primary space-y-6 flex flex-col justify-center">
        <h2 class="h2 mb-6 md:text-[48px]">SHARE YOUR INSTANTS</h2>
        <p>➚ Take a photo of your instant with your smartphone (or scan it for max. quality) – the file should be between 2–10 MB, in jpg/jpeg or webp format.</p>
        <p>➚ Upload your photo and credentials in the form below.</p>
        <p>➚ Agree to our terms and conditions [anchor til “terms and conditions” sektionen]. <br/> Short version: Participants have to be 18+, portrayed persons must have given consent, and you agree that your instant can be exhibited and used to promote the photo contest.</p>
        <p>➚ You can make up to 5 submissions – go through step 1–3 for each submission ;-)</p>
      </div>

      <!-- Form -->
      <form class="grid grid-cols-1 md:grid-cols-2 gap-6 content-start">
        <!-- Full Name -->
        <div>
          <label for="fullname" class="h4 text-primary block mb-2">FULL NAME</label>
          <input type="text" id="fullname" name="fullname" placeholder="Enter your display name"
            class="w-full border border-accent p-3 focus:ring-2 focus:ring-primary focus:outline-none" />
        </div>
        <!-- Email -->
        <div>
          <label for="email" class="h4 text-primary block mb-2">E-MAIL</label>
          <input type="email" id="email" name="email" placeholder="Enter your email"
            class="w-full border border-accent p-3 focus:ring-2 focus:ring-primary focus:outline-none" />
        </div>

        <!-- Upload photo (full width) -->
        <div class="md:col-span-2">
          <label for="photo" class="h4 text-primary block mb-2">UPLOAD PHOTO</label>
          <div class="border-2 border-dashed border-accent p-10 flex flex-col items-center justify-center text-center cursor-pointer hover:bg-accent/30">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-14 w-14 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 15a4 4 0 014-4h14a2 2 0 012 2v6a2 2 0 01-2 2H7a4 4 0 01-4-4zM7 10V6a4 4 0 118 0v4" />
            </svg>
            <input type="file" id="photo" name="photo" class="hidden" />
            <span class="p-small mt-4 text-primary">Click to upload or drag and drop</span>
          </div>
        </div>

        <!-- Submission title (full width) -->
        <div class="md:col-span-2">
          <label for="title" class="h4 text-primary block mb-2">SUBMISSION TITLE</label>
          <input type="text" id="title" name="title" placeholder="input type text"
            class="w-full border border-accent p-3 focus:ring-2 focus:ring-primary focus:outline-none" />
        </div>

        <!-- Description (full width) -->
        <div class="md:col-span-2">
          <label for="description" class="h4 text-primary block mb-2">SUBMISSION DESCRIPTION/ STORY</label>
          <p class="p-small text-primary mb-2">Describe the image and how you took it in a sentence or two.</p>
          <textarea id="description" name="description" rows="4"
            class="w-full border border-accent p-3 focus:ring-2 focus:ring-primary focus:outline-none"></textarea>
        </div>

        <!-- Terms (full width) -->
        <div class="md:col-span-2 flex items-start space-x-3">
          <input type="checkbox" id="terms" name="terms"
            class="mt-1 border-accent text-primary focus:ring-primary" />
          <label for="terms" class="p-small text-primary">I have read and agree to the terms and conditions</label>
        </div>

        <!-- Submit (full width) -->
        <div class="md:col-span-2">
          <button type="submit"
            class="w-full bg-primary text-secondary font-semibold py-4 px-8 hover:bg-primary/90 transition">
            Submit Now
          </button>
        </div>
      </form>
    </div>
  </section>
</MainLayout>