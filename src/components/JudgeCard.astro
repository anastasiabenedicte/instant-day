---
const { name, role, image } = Astro.props;
---

<div class="flex flex-col items-center text-center gap-4">
  <img 
    src={image} 
    alt={name} 
    class="w-60 h-60 object-cover shadow-md" 
    style="border-radius: 0;" 
  />
  <h3 class="h3 text-primary">{name}</h3>
  <p class="p text-primary">{role}</p>
</div>
