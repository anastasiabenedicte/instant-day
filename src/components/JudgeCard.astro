---
const { name, role, image } = Astro.props;
---

<div class="flex flex-col items-start text-left gap-2 max-w-xs">
  <img 
    src={image} 
    alt={name} 
    class="w-full object-cover shadow-md"
  />
  <h3 class="h3 text-primary">{name}</h3>
  <p class="p text-primary">{role}</p>
</div>